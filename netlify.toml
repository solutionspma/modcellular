[build]
  base = "mod_web"
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for routes that don't match a file (assets served normally)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
